import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
      flex: 1,
    },
    notificationHeader: {
      gap:16,
      marginLeft: 0,
      paddingHorizontal: 24,
      paddingVertical: 16,
    },
    FlatList: {
      flex:1,
    },
  });